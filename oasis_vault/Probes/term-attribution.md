---
id: probe.term-attribution
type: probe
status: asserted
domain: ordering
title: Formula term attribution
guards: []
tests: [claim.ordering.sigma-L-missing, claim.ordering.z-over-engineered]
---

**Entrypoint:** none yet — this probe is a placeholder.

Decompose residual-cover error across dR, dL, dsigma_L, dz. Must reproduce the known ranking (R 2.14x > sigma_L > z 1.52x) before it is trusted to rank anything new.

TO BUILD: `devkit/probe_term_attribution.py`.
