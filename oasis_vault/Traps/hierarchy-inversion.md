---
id: trap.hierarchy-inversion
type: trap
status: trusted
domain: method
title: Hierarchy inversion
code: T6
guards: [probe.backtest-allocation]
---

A fix that measures clean can still invert the design. The rebuilt department weights scored well and halved the staple share (60.7% -> 31.3%). A working guard read as a symptom.

**Detector:** `traps.rank_diff(before, after, hierarchy=...)` — diff the resulting RANK ORDER against the hierarchy the operating docs encode, not just the loss. Read the operating docs before changing weights that encode a hierarchy.
