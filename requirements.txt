# OASIS direct dependencies — pinned to the versions verified in production.
# Full transitive snapshot lives in requirements.lock.txt (pip freeze).
# To upgrade: bump here, reinstall, run pytest, re-freeze the lock file.

# Core Data & UI
pandas==2.3.3
streamlit==1.55.0
numpy==1.26.4
openpyxl==3.1.5
plotly==6.6.0
altair==4.2.2
python-dateutil==2.9.0.post0
httpx==0.28.1
sqlalchemy==2.0.48
apscheduler==3.11.2
pydeck==0.9.1
xlsxwriter==3.2.9

# API & Backend
fastapi==0.135.1
uvicorn==0.42.0
pydantic==2.12.5
python-dotenv==1.2.2
python-multipart==0.0.26
requests==2.32.5

# Auth
bcrypt==5.0.0

# Database migrations & PostgreSQL backend (OASIS_DB_URL=postgresql://...)
alembic==1.18.5
psycopg2-binary==2.9.12

# AI & GNN (CPU-only build for lightweight deployment)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
anthropic==0.85.0
scipy==1.15.3
nest-asyncio==1.6.0

# Shadow Monitor & File Watcher
watchdog==6.0.0

# Document Generation
python-docx==1.2.0

# Maps & Geospatial (ST-GAT)
folium==0.20.0
streamlit-folium==0.26.2

# ML & Clustering (ST-GAT)
scikit-learn==1.7.2
joblib==1.5.3

# Desktop App (Flet)
# 0.26+ is REQUIRED, not preferred: oasis/desktop is written against the
# ft.Icons / ft.Colors enum API that replaced ft.icons / ft.colors. On 0.25.2
# every view raises AttributeError on import — the pin below said 0.25.2 while
# the code needed 0.28, so the declared environment could not run the app.
# flet-desktop provides the native window used by `--mode desktop`.
# (Packaging the .exe additionally needs flet-cli, a BUILD-time tool — see
# build_desktop.py; it is deliberately not a client runtime dependency.)
flet==0.28.3
flet-desktop==0.28.3

# SQL Server Integration (Optional - install only if using MSSQL)
# pyodbc==5.2.0
fpdf2==2.8.7
