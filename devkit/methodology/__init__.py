"""Methodology interrogation toolkit.

The graph over OASIS's own methodology: claims, parameters, decisions, probes,
evidence and traps. Deterministic, stdlib-only, and readable in git.

Stdlib only ON PURPOSE. This runs under the project's Windows venv, under the
Linux dev VM, and in CI with no install step. The one thing a self-correcting
loop must not do is fail to run.
"""
